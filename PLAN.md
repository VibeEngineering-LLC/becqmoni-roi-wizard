# План доработки ROI-мастера по замечаниям рецензента (25.07.2026)

Объект: `https://github.com/VibeEngineering-LLC/becqmoni-roi-wizard`
(live: https://vibeengineering-llc.github.io/becqmoni-roi-wizard/).
Работа — фазами, коммит и прогон после каждой.

## СТАТУС на 25.07.2026 (сессия «Цензор ноутбук»)

- **Фаза 1 — СДЕЛАНА** (+ указание стандарта по замечанию рецензента). Опубликованный HEAD
  до неё: `565aadb`.
- **Фаза 3 — СДЕЛАНА** (критерии слияния, якорь, полный набор, дефолт I = 3 %).
- **Фаза 2 — СДЕЛАНА** (комбобокс групп, кнопка (i), живой поиск).
- **Фаза 4 — СДЕЛАНА иначе, чем планировалась:** вместо `SPEC.md` под будущий порт написан
  сам модуль (`integration/`, C#, .NET Framework 4.8, WinForms). Спецификацией служит код
  плюс `integration/README.md` — там же формулы, XML-контракты и раздел «что важно
  не сломать». Форма интеграции выбрана однооконная со вкладками (см. ниже).
- Решения оператора: в RU основная — российская классификация, ANSI-код в скобках (в EN наоборот).

**Дальше плана (25.07):** три волны проверок — самоаудит, внешний аудит и проверка
сборкой против дерева BecqMoni; 14 находок, все закрыты; тесты — 71 проверка.
План внесения в upstream — `docs/UPSTREAM-PR.md`.

## Ответы рецензента (25.07.2026) и что из них следует

**1. Классификация.** «Да, стандартизированная, просто в некоторых источниках идёт как SPEC,
в некоторых SNM — стоит указать какой стандарт взял за основу. POPULAR — не уверен насчёт Cs-134.
В целом — да.»

→ В базу добавлено `meta.fam_standard_ru/_en`: за основу взят **ANSI N42.34** (NORM / MED / IND /
**SNM**), оговорено, что часть источников пишет SPEC; FISS / NAA / WASTE помечены как группы вне
стандарта. Текст показывается в словарике кнопки **(i)** (фаза 2).
Cs-134 в POPULAR **оставлен**: это рабочий маркер свежего осколочного загрязнения (пара с Cs-137
даёт возраст выброса), а «популярный» здесь = «часто ищут», а не «часто желателен».

**2. Пороги якорного режима.** «Второй предел с якорем — без тестов не отвечу, нужно гонять
RjmcmcTuning… ΔE_min = 0.1·FWHM — начни с 0.2 до 0.3, где-то так будет по тестам… Выше — не надо,
там уже и так работает деконволюция + сэты.»

→ Тесты **не требуются**: прикидка 0.2–0.3 совпадает с константой, уже вкомпилированной в боевой
код. Первоисточник — `BecquerelMonitor/LibraryPeakFitter.cs` (master):

| Константа | Значение | Строка | Смысл |
|---|---|---|---|
| `ClaimToleranceFwhm` | **0.25·FWHM** | :43 | пик «принадлежит» линии — линия считается уже найденной |
| `SparrowFwhm` | **0.85·FWHM** | :51 | кластеризация линий одной цепочки в BR-группу (δ = 2σ = 0.849·FWHM) |
| `AnchorMatchToleranceFwhm` | 0.5·FWHM | :38 | якорная линия сета ↔ найденный пик |
| `BlendCoverToleranceFwhm` | 0.5·FWHM | :47 | пик признаётся центроидом бленда и заменяется линиями группы |
| `SignificanceZ` | **z ≥ 4** | :35 | гейт значимости фитованной амплитуды (Fisher z), **не** порог интенсивности |

Отсюда — итоговая физика мастера (фаза 3): **два разных режима, а не один порог.**

- **ROI-маркеры (для глаз / слепого поиска):** сливать при `ΔE < 0.849·FWHM` — предел Sparrow,
  такие пары finder не разрешает в принципе.
- **Библиотечный сет (с якорем):** сливать только при `ΔE < 0.25·FWHM` (настройка 0.2–0.3).
  **Выше не сливать** — 0.25…0.85 FWHM разрешает BR-связка `LibraryPeakFitter`, а
  выше 0.85 FWHM — деконволюция. Прореживание сета вредит: по README RjmcmcTuning
  (итерация 9) полный ряд в сете даёт Th 26/29 и Ra **27/27**, а «реальный сет пользователя
  из 16 сильных линий» — нулевой прирост.

**Найдено в исходниках, важно для экспорта (правится в фазе 3):**

- `LibraryPeakFitter` **не запускается вовсе**, если в наборе нет ни одной линии с `IsAnchor=true`
  (`if (setLines.Count == 0 || !setLines.Any(n => n.IsAnchor)) return`, :131). Наш экспорт сейчас
  пишет `IsAnchor=false` всем линиям (`index.html:865`) — **выгруженный набор для библиотечного
  фита нерабочий**. Приоритетный фикс.
- Цепочка для BR-связки берётся из **последних скобок имени**: `«Bi-214 (Ra-226)» → «Ra-226»`
  (`ChainOf`, :502). Наши режимы «цепочкой» (`Bi-214 (U-238)`) и «одним изотопом» (все линии под
  именем родителя) оба дают единый идентификатор — совместимы; а набор из отдельно добавленных
  нуклидов без скобок BR-связку не получит.
- В группу берутся только линии с `Intencity > 0` у **всех** членов (:255), веса ∝ `Intencity` —
  значит экспортировать нужно равновесные абсолютные интенсивности (наш пересчёт `EQ_FACTOR`),
  а не относительные внутри изотопа.
- Вторичные пики (SE/DE/BS/SUM) в библиотечный набор класть нельзя: `LibraryPeakFitter` сажает
  escape-компоненты сам (:296), а имя вида `SE (Bi-214)` создаст ложную «цепочку».

## 0. Сводка замечаний

| № | Замечание | Тип | Фаза |
|---|---|---|---|
| 1 | Семейства назвать общепринятыми кодами (NORM, SPEC/SNM, MED …) + кнопка **(i)** со словариком | классификация | 1 |
| 2 | Семейство выбирать **комбобоксом**, не разворачивать весь список сразу | UI | 2 |
| 3 | Типовые цепочки вынести отдельным семейством **POPULAR** | классификация | 1 |
| 4 | Учесть **предел Sparrow = 0.8·FWHM** на энергии линии | физика | 3 |
| 5 | Учесть предел **с якорным пиком**: два предела, один ≈ 0.1·FWHM (неточно), второй — по интенсивностям | физика | 3 |
| 6 | Поиск должен **фильтровать** список, а не показывать всё | UI | 2 |
| 7 | Дефолт фильтра интенсивности — **3 %** (для разрешения ≈ 6 %) | параметр | 3 |
| 8 | Форма интеграции в BecqMoni: визард против однооконной формы | проектное | 4 |

---

## Фаза 1 — классификация нуклидов ✅ СДЕЛАНО (коммит b724bb9)

Правится каталог `CAT`/`FAMILIES` в `update_nuclides.py`, база перегенерируется.

**Что фактически сделано и проверено в браузере:**
- ключи семейств: `popular, norm, med, ind, snm, fiss, naa, waste` (были `erh/med/fission/tech/naa/waste`);
- названия — из базы: RU «ЕРН — естественные (NORM)», «Техногенные и калибровочные (IND)»…,
  EN «NORM (naturally occurring)», «IND (industrial)»…; в `index.html` `famTitle/chainTitle`
  читают их из `NUCLIDE_DB`, хардкод `FAM_EN`/`CHAIN_EN` удалён;
- добавлены `info_ru`/`info_en` на каждое семейство (тексты для кнопки (i), фаза 2);
- **POPULAR (12)**: U-238, Ra-226, Th-232, K-40, Tc-99m, I-131, Cs-137, Cs-134, Co-60, Ba-133, Eu-152, Am-241;
- **SNM (3)**: U-235, Pu-239, Pu-241;
- у рядов появились `title_en`; база перегенерирована — 121 нуклид, старых ключей не осталось.

- [x] Указан стандарт кодов: `meta.fam_standard_ru/_en` в базе (ANSI N42.34; оговорка SPEC/SNM;
      FISS/NAA/WASTE — вне стандарта). Показывается в словарике (i).
- [x] Cs-134 в POPULAR оставлен (обоснование выше).

- [x] Перевести семейства на коды стандарта ANSI N42.34 (принят в RIID-приборах), русские расшифровки сохранить:

| Сейчас | Станет | Расшифровка (в словарик (i)) |
|---|---|---|
| ЕРН (естественные) | **NORM** | Naturally Occurring Radioactive Material — природные |
| Медицинские | **MED** | Medical — диагностика и терапия |
| Техногенные и калибровочные | **IND** | Industrial — промышленные и калибровочные источники |
| — (выделить из «отходов») | **SNM** | Special Nuclear Material — делящиеся: U-235, Pu-239, Pu-241 |
| Осколки деления | **FISS** | Fission products — продукты деления |
| Нейтронная активация | **NAA** | Neutron activation — активационные |
| Ядерные отходы / ОЯТ | **WASTE** | ОЯТ, РАО, минорные актиниды |
| — (новое) | **POPULAR** | Готовые типовые наборы: три ряда ЕРН + топ-нуклиды поля |

- [x] Заведён **POPULAR**: ряды U-238 / Th-232 / U-235 как готовые пункты + K-40, Cs-137, Cs-134,
      Co-60, Am-241, Ra-226, I-131, Tc-99m, Ba-133, Eu-152 *(состав — на утверждение)*.
- [x] Выделен **SNM** из WASTE (U-235, Pu-239, Pu-241; U-233 — при необходимости добавить в базу).
- [x] Словарь расшифровок — в `data/nuclides.js` (чтобы (i) не хардкодился в разметке).

**Приёмка:** база перегенерирована, число нуклидов не уменьшилось, каждый нуклид сохранил прежние
семейства (переименование, а не переразметка), POPULAR и SNM непусты.

---

## Фаза 2 — UI блока «Изотопы, семейства, цепочки» ✅ СДЕЛАНО

- [x] **Комбобокс «Группа»** (семейства + ряды в одном списке): выбор → под ним список только его членов. Экономит ~половину высоты блока.
- [x] Цепочки — пунктами того же комбобокса (раздел «Ряды»), кнопки «одним / цепочкой» сохраняются.
- [x] **Кнопка (i)** — раскрывающийся словарик кодов (NORM/MED/IND/SNM/FISS/NAA/WASTE/POPULAR).
- [x] **Живой поиск**: ввод фильтрует список нуклидов (совпадение по имени), показываются только
      совпавшие, клик по строке = добавить. Сейчас `datalist` + кнопка «Добавить» — список не сужается.
      В строке результата: имя, T½, семейства, число γ/X-линий.

**Приёмка:** при пустом поиске список нуклидов не занимает экран; ввод «th» оставляет только Th-*;
выбор семейства в комбобоксе меняет список членов; (i) открывает словарик; RU/EN переключение не ломает.

---

## Фаза 3 — физика слияния линий и параметры по умолчанию ✅ СДЕЛАНО

Числа — из `LibraryPeakFitter.cs` и ответа рецензента (таблица выше), не гипотезы.
**Статус: сделано 25.07.** Проверено в браузере на ряде Th-232 при R = 6 %:
якорный режим (0.25·FWHM ≈ 12 кэВ на 966 кэВ) даёт группы 904–911, 944–948, 959–969, 976–988 —
дублет 964.8/969.0 слит, 911.2 отдельно; Sparrow (0.85·FWHM ≈ 41 кэВ) собирает 948–989.
Автовыбор якоря: Th-232 → 2614.5 (Tl-208), U-238 → 609.3 (Bi-214) — те же линии, что в прогонах
RjmcmcTuning. В выгрузке ровно один `IsAnchor=true`, интенсивностей 0 нет, суффикс цепочки един.

- [x] **Селектор режима слияния** вместо одного `k`:
      1. **Sparrow (маркеры ROI)** — дефолт для ROI: `ΔE < 0.849·FWHM(E)` (δ = 2σ; в коде BecqMoni 0.85).
      2. **Якорный набор (библиотечный фит)** — `ΔE < 0.25·FWHM`, поле в диапазоне 0.2–0.3.
         Выше не сливать: 0.25…0.85 разрешает BR-связка, выше — деконволюция.
      3. **Ручной `k×FWHM`** — третьим вариантом, как сейчас.
      `FWHM(E) = R₆₆₂·√(662·E)/100`. Предела по отношению интенсивностей нет — в BecqMoni эту роль
      играет статистический гейт `z ≥ 4`, зависящий от набранной статистики, а не константа.
- [x] **`IsAnchor=true`** в экспорте библиотеки: выбор якорной линии в UI (дефолт — сильнейшая линия
      набора; для рядов — 2614.5 Tl-208 / 609.3 Bi-214). Без этого `LibraryPeakFitter` не стартует.
- [x] **Профиль экспорта «полный набор для библиотечного фита»**: без фильтра по интенсивности,
      без слияния (кроме < 0.25·FWHM), равновесные абсолютные `Intencity` (`EQ_FACTOR` включён),
      вторичные пики исключены, имена с единым суффиксом цепочки.
- [x] Дефолт фильтра интенсивности для **ROI-маркеров** — **3 %** (относительная, R ≈ 6 %);
      на профиль библиотеки не распространяется.

**Приёмка:** переключение режима меняет число объединений предсказуемо (Sparrow ≫ якорный);
при R = 6 % на 966 кэВ FWHM ≈ 48 кэВ, порог якорного режима 0.25·FWHM ≈ 12 кэВ — дублет Ac-228
964.8/969.0 (Δ 4.2 кэВ) сливается, пара 911.2/969.0 (Δ 57.8 кэВ) — нет;
в выгруженном `NuclideDefinition.xml` ровно одна линия с `IsAnchor=true` на набор,
`Intencity > 0` у всех линий, суффикс цепочки одинаковый.

---

## Фаза 4 — форма интеграции в BecqMoni (проектное решение)

Вопрос: визард (по шагам) против однооконной формы.

**Довод против визарда** (озвучен рецензентом и подтверждается практикой): возврат на несколько
шагов назад. Шаги здесь **не линейны** — реальный цикл: выбрал нуклиды → увидел линии → подкрутил
фильтр/разрешение → выбор линий переехал → вернулся добавить пропущенное → снова фильтры.
В визарде это 3–4 «назад» на каждой итерации.

**Рекомендация — однооконная форма с секциями-шагами** (как в прототипе), плюс:
- нумерованные секции с подсветкой активной — «пошаговость» без потери контекста;
- сворачивание неактивных секций — фокус, ради которого обычно и берут визард;
- всегда видимый счётчик результата (сколько линий выбрано) — обратная связь без перехода на «финиш».

**Визард — как надстройка**, не как архитектура: «мастер за 3 шага» поверх той же формы,
состояние общее, выход в полную форму в любой момент. Дешёвая добавка, если понадобится.

- [x] ~~`SPEC.md` под будущий порт~~ — не понадобился: порт сделан сразу кодом.
      Шаги, поля, формулы (FWHM(E), Sparrow, равновесие ряда, вторичные пики BS/CE/SE/DE/SUM/PU)
      и XML-контракты (ROI `FormatVersion 120920`; `NuclideDefinitionFile` = `NuclideDefinitions`
      + `Sets`/guid + `IsAnchor` + `NuclideSets`) описаны в `integration/README.md` и
      реализованы в `integration/BecquerelMonitor/RoiWizard/`.
- [x] Форма интеграции выбрана: **однооконная, со вкладками-шагами** («1 · Изотопы»,
      «2 · Линии», «3 · Оформление и экспорт») — визарда с переходами «назад» нет.

---

## Открытые вопросы

1. ~~SPEC = SNM?~~ — закрыт 25.07: принят ANSI N42.34, стандарт указан в базе и в (i).
2. ~~Состав POPULAR~~ — закрыт 25.07: принят, Cs-134 оставлен.
3. ~~Якорный режим: 0.1·FWHM и правило по интенсивностям~~ — закрыт 25.07: `0.25·FWHM`
   (диапазон 0.2–0.3), выше не сливать; правила по отношению интенсивностей нет,
   его роль в BecqMoni играет гейт `z ≥ 4`.
4. ~~Форма интеграции в BecqMoni (фаза 4) — визард против однооконной формы~~ — закрыт 25.07:
   однооконная форма со вкладками-шагами, реализована в `integration/`.

**Открыто сейчас:**

- Режим «полный набор» (все линии минуя фильтры) в модуле не сделан — в вебе он есть и
  именно он даёт лучший recall.
- Якорь в модуле ровно один, а `LibraryPeakFitter` умеет работать с несколькими.
- Форма PR в upstream и вопросы владельцу — `docs/UPSTREAM-PR.md`.

## Порядок работ

1. Фаза 1 (каталог + регенерация базы) → коммит.
2. Фаза 2 (комбобокс, (i), живой поиск) → коммит.
3. Фаза 3 (Sparrow / якорь / дефолты / IsAnchor) → коммит.
4. Фаза 4 (`SPEC.md`) — после подтверждения решения по форме интеграции.

Публикация каждой фазы: пуш в `main` → GitHub Pages пересобирается автоматически.
