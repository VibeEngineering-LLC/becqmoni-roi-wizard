# Таблица полей ROI-мастера

Полный перечень элементов управления: `id`, тип, единицы, значение по умолчанию,
на что влияет. Логика привязана к этим `id` — переименование ломает страницу.
Оформление описано отдельно: `styles/README.md`.

## Шаг 1 · Изотопы

| id | Тип | По умолчанию | Назначение |
|---|---|---|---|
| `nucSearch` | текст | — | живой поиск по имени нуклида или коду семейства |
| `nucAdd` | кнопка | — | добавить найденный нуклид (только его линии) |
| `nucAddF` | кнопка | — | + линии дочерних под именем родителя (один изотоп) |
| `nucAddC` | кнопка | — | + дочерние отдельными нуклидами, родитель в скобках |
| `searchList` | список | 121 строка | результат поиска; клик по строке добавляет |
| `famSel` | комбобокс | POPULAR | группа: 8 семейств + 3 ряда распада |
| `famCtl` | кнопки | — | «добавить все» / «+ семейство» / «+ цепочка» для группы |
| `famInfoBtn` / `famInfoBox` | кнопка / попап | скрыт | словарик кодов семейств и стандарта |
| `famList` | список | члены группы | чекбоксы членов; серым — без линий выше порога |
| `xrfList` | список | 10 элементов | ХРИ материалов защит и детекторов |
| `selBar` | чипы | пусто | выбранные нуклиды и элементы, «×» удаляет |
| `clearAll` | кнопка | — | очистить выбор целиком |
| `presetBar` | ссылки в меню | 5 пресетов | готовые наборы; ставят порог I = 3 % |

## Шаг 2 · Линии

| id | Тип | По умолчанию | Единицы | Назначение |
|---|---|---|---|---|
| `mR662` | число | 7,5 | % | разрешение детектора FWHM на 662 кэВ |
| `mMode` | комбобокс | `sparrow` | — | критерий: Sparrow 0,85 · якорный 0,25 · вручную |
| `mK` | число | 0,85 | доли FWHM | порог ΔE; в якорном режиме 0,2–0,3 |
| `mergeBtn` / `unmergeBtn` | кнопки | — | — | объединить близкие линии / вернуть исходные |
| `mergeStat`, `mergeHint`, `mergeInfo` | текст | — | — | сколько групп слито, порог в кэВ |
| `fIOn` + `fMinI` + `iMode` | галка + число + комбобокс | выкл, 3, отн. | % | фильтр по интенсивности |
| `fEOn` + `fEmin` + `fEmax` | галка + числа | выкл, 10, 3000 | кэВ | фильтр по энергии |
| `fTOn` + `fTmin`/`fTminU` + `fTmax`/`fTmaxU` | галка + числа + единицы | выкл, 1 сут, ∞ | с/ч/сут/лет | фильтр по периоду полураспада |
| `.fT` (4 шт.) | галки | все вкл | — | типы линий: γ, X, ХРИ, вторичные |
| `sortSel` | комбобокс | энергия ↑ | — | сортировка таблицы |
| `selVisible` / `unselVisible` | кнопки | — | — | выбрать / снять все видимые |
| `topN` + `selTop` | число + кнопка | 5 | шт. | оставить топ-N линий на нуклид |
| `hideUnsel` | галка | выкл | — | скрыть невыбранные строки |
| `eqScale` | галка | вкл | — | равновесие ряда: I на распад родителя |
| `secMinI` + `.secT` (8 шт.) + `genSec` | число + галки + кнопка | 10 %, BS/SE/DE | % | вторичные пики |
| `nearE`, `nearW`, `nearI`, `nearT`/`nearTu`, `nearBtn` | числа + кнопка | 362, ±10, 0,5 %, 0 | кэВ, % | поиск близких линий по всей базе |
| `lineTbl` | таблица | — | — | ✓ · Нуклид · E, кэВ · I, % · I отн., % · T½ · Тип |

## Шаг 3 · Оформление ROI

| id | Тип | По умолчанию | Назначение |
|---|---|---|---|
| `roiMode` | комбобокс | маркеры | маркеры · зоны · зоны + маркеры |
| `wMode` | комбобокс | % от E | способ задания ширины зоны |
| `wPct` | число | 5 % | ширина зоны в процентах от энергии |
| `r662` + `kF` | числа | 7,5 % / 3 | ширина зоны как k × FWHM |
| `colByChain` / `colByNuc` | кнопки | по цепочке | схема раскраски |
| `colorBox` | палитра | — | цвет каждой цепочки/нуклида |
| `specBar` | canvas | — | предпросмотр набора на энергетической оси |

## Шаг 4 · Экспорт

| id | Тип | По умолчанию | Назначение |
|---|---|---|---|
| `cfgName` | текст | IAEA lines | имя ROI-конфигурации (и заголовок окна) |
| `btnXml` / `btnXml2` | кнопки | — | скачать ROI `.xml` (FormatVersion 120920) |
| `btnPreview` / `xmlPrev` | кнопка / блок | скрыт | предпросмотр XML |
| `libName` | текст | Новый набор | имя NuclideSet в библиотеке |
| `anchorSel` | комбобокс | авто | якорная линия — единственная с `IsAnchor=true` |
| `libFull` | галка | выкл | полный набор: все линии, минуя фильтры |
| `bakOrig` | галка | вкл | скачать копию оригинального файла библиотеки |
| `btnNucXml` / `btnNucXml2` + `mergeFile` | кнопки + файл | — | дописать набор в `NuclideDefinition.xml` |
| `expStat` | текст | — | сколько линий уйдёт в ROI и в набор |

## Общие элементы

| id | Назначение |
|---|---|
| `tabs` | закладки шагов; `b` внутри кнопки — счётчик состояния |
| `menubar` | Файл · Набор · Данные · Справка → переход на нужный шаг |
| `dbinfo` | сводка базы: нуклидов, дата снимка, пороги |
| `sumBar` | статусная строка: показано/выбрано/слито/якорь |
| `stepPrev` / `stepNext` | навигация по шагам, подписаны соседними шагами |
| `langBtn` | RU/EN |
| `toast` | сообщение после экспорта |

## Данные

`data/nuclides.js` — снимок IAEA (121 нуклид): `nuclides[имя] = {g:[[E,I]], x:[[E,I,оболочка]],
fam:[коды], chain, hl_s, hl_y, hl_txt}`, плюс `families`, `chains`, `meta`.
`data/xrf.js` — `XRF_DB[элемент] = {Z, ctx, ctx_en, lines:[[линия,E,I]]}`.
Оба файла генерируются `update_nuclides.py`; руками не править.
